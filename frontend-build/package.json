{
  "name": "frontend-build",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "dockerlint -p",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "dockerlint": "^0.3.9"
  }
}
